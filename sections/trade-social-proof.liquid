{%- comment -%}
  sections/trade-social-proof.liquid
  Section header + 2-column grid of _trade-quote blocks (1-column mobile).
{%- endcomment -%}

<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div class="trade-social section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }}">
  <div class="trade-social__header">
    {%- if section.settings.eyebrow != blank -%}
      <p class="trade-social__eyebrow">{{ section.settings.eyebrow | escape }}</p>
    {%- endif -%}
    {%- if section.settings.heading != blank -%}
      <h2 class="trade-social__heading">{{ section.settings.heading | escape }}</h2>
    {%- endif -%}
  </div>
  <div class="trade-social__grid">
    {% content_for 'blocks' %}
  </div>
</div>

{% stylesheet %}
  .trade-social {
    padding-block: clamp(48px, 6vw, 80px);
  }
  .trade-social__header {
    text-align: center;
    max-width: 720px;
    margin-inline: auto;
    margin-block-end: 56px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  .trade-social__eyebrow {
    font-family: var(--font-accent--family);
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: var(--color-foreground-muted);
    margin: 0;
  }
  .trade-social__heading {
    font-family: var(--font-heading--family);
    font-size: clamp(28px, 3.4vw, 44px);
    font-weight: 600;
    line-height: 1.1;
    margin: 0;
    color: var(--color-foreground-heading);
  }
  .trade-social__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
  }
  @media screen and (min-width: 750px) {
    .trade-social__grid {
      grid-template-columns: 1fr 1fr;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Trade Social Proof",
  "tag": "section",
  "class": "trade-social-wrapper section-wrapper",
  "disabled_on": { "groups": ["header"] },
  "blocks": [ { "type": "_trade-quote" } ],
  "max_blocks": 2,
  "settings": [
    { "type": "header", "content": "Section header" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "From the Field" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "What our trade members are saying." },
    { "type": "header", "content": "Section appearance" },
    {
      "type": "select",
      "id": "section_width",
      "label": "Width",
      "options": [
        { "value": "page-width", "label": "Page width" },
        { "value": "full-width", "label": "Full width" }
      ],
      "default": "page-width"
    },
    { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-trade-cream" }
  ]
}
{% endschema %}
