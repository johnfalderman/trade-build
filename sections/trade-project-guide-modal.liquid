{%- comment -%}
  sections/trade-project-guide-modal.liquid
  Email/phone gate for the Trade Project Guide PDF.
  Pattern is identical to trade-lookbook-modal.liquid — opened by any
  element on the page whose href is #trade-project-guide (project type
  tiles, "Download Our Project Guide" CTA link).

  Uses native {% form 'customer' %} so the lead is captured as a
  Shopify customer record tagged trade-project-guide-lead. On submit
  the page reloads; if this form posted successfully, we render an
  auto-open flag the inline script reads on DOMContentLoaded so the
  modal re-opens in its success state with the PDF link.

  Gotchas (same as lookbook modal):
  - pdf_url must be type: text not type: url (Shopify rejects defaults
    on url-type settings)
  - Modal added once to page section order, renders once per page
  - Dialog id must match href target: id="trade-project-guide"
{%- endcomment -%}

{%- assign pdf_url = section.settings.pdf_url -%}

<dialog
  id="trade-project-guide"
  class="s6trade-pg-modal"
  aria-labelledby="s6trade-pg-heading"
>
  <div class="s6trade-pg-modal__panel" role="document">
    <button
      type="button"
      class="s6trade-pg-modal__close"
      aria-label="Close"
      data-trade-pg-close
    >
      <svg viewBox="0 0 20 20" width="20" height="20" aria-hidden="true" focusable="false">
        <path d="M4 4l12 12M16 4L4 16" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"/>
      </svg>
    </button>

    {%- form 'customer', class: 's6trade-pg-modal__form' -%}
      <input type="hidden" name="contact[tags]" value="trade-project-guide-lead">

      {%- if form.posted_successfully? -%}
        <div class="s6trade-pg-modal__success" data-trade-pg-success>
          <h2 id="s6trade-pg-heading" class="s6trade-pg-modal__heading">
            {{ section.settings.success_heading | escape }}
          </h2>
          <div class="s6trade-pg-modal__body">{{ section.settings.success_body }}</div>
          {%- if pdf_url != blank -%}
            <a
              class="s6trade-pg-modal__download"
              href="{{ pdf_url }}"
              target="_blank"
              rel="noopener"
            >
              {{ section.settings.success_link_label | escape }}
            </a>
          {%- endif -%}
        </div>
      {%- else -%}
        <p class="s6trade-pg-modal__eyebrow">{{ section.settings.eyebrow | escape }}</p>
        <h2 id="s6trade-pg-heading" class="s6trade-pg-modal__heading">
          {{ section.settings.heading | escape }}
        </h2>
        <div class="s6trade-pg-modal__body">{{ section.settings.description }}</div>
        <p class="s6trade-pg-modal__microcopy">{{ section.settings.microcopy }}</p>

        <div class="s6trade-pg-modal__fields">
          <label class="s6trade-pg-modal__field">
            <span class="s6trade-pg-modal__label">Email</span>
            <input
              type="email"
              name="contact[email]"
              autocomplete="email"
              autocorrect="off"
              autocapitalize="off"
              required
            >
          </label>
          <label class="s6trade-pg-modal__field">
            <span class="s6trade-pg-modal__label">Phone (optional)</span>
            <input
              type="tel"
              name="contact[phone]"
              autocomplete="tel"
            >
          </label>
        </div>

        {%- if form.errors -%}
          <div class="s6trade-pg-modal__error" role="alert">
            {{- form.errors | default_errors -}}
          </div>
        {%- endif -%}

        <button type="submit" class="s6trade-pg-modal__submit">
          {{ section.settings.submit_label | escape }}
        </button>

        <p class="s6trade-pg-modal__fineprint">{{ section.settings.fineprint | escape }}</p>
      {%- endif -%}
    {%- endform -%}
  </div>
</dialog>

{% stylesheet %}
  .s6trade-pg-modal {
    padding: 0;
    border: none;
    background: transparent;
    max-width: 560px;
    width: calc(100vw - 32px);
    color: var(--color-foreground);
  }
  .s6trade-pg-modal::backdrop {
    background: rgba(20, 18, 14, 0.55);
  }
  .s6trade-pg-modal[open] {
    display: block;
  }
  .s6trade-pg-modal__panel {
    position: relative;
    background: #faf6ef;
    padding: clamp(28px, 4vw, 44px);
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.25);
  }
  .s6trade-pg-modal__close {
    position: absolute;
    top: 12px;
    right: 12px;
    background: transparent;
    border: none;
    padding: 8px;
    cursor: pointer;
    color: var(--color-foreground);
    line-height: 0;
  }
  .s6trade-pg-modal__close:hover {
    color: var(--color-foreground-heading);
  }
  .s6trade-pg-modal__form {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .s6trade-pg-modal__eyebrow {
    font-family: var(--font-accent--family);
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: var(--color-foreground-muted);
    margin: 0;
  }
  .s6trade-pg-modal__heading {
    font-family: var(--font-heading--family);
    font-size: clamp(24px, 2.6vw, 30px);
    font-weight: 600;
    line-height: 1.15;
    margin: 0;
    color: var(--color-foreground-heading);
  }
  .s6trade-pg-modal__body {
    font-family: var(--font-body--family);
    font-size: 16px;
    line-height: 1.55;
  }
  .s6trade-pg-modal__body :where(p) { margin: 0; }
  .s6trade-pg-modal__microcopy {
    font-family: var(--font-body--family);
    font-size: 14px;
    line-height: 1.5;
    color: var(--color-foreground-muted);
    margin: 4px 0 0;
  }
  .s6trade-pg-modal__fields {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-block-start: 8px;
  }
  .s6trade-pg-modal__field {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .s6trade-pg-modal__label {
    font-family: var(--font-body--family);
    font-size: 13px;
    font-weight: 500;
    color: var(--color-foreground);
  }
  .s6trade-pg-modal__field input {
    font-family: var(--font-body--family);
    font-size: 15px;
    padding: 12px 14px;
    border: 1px solid var(--color-border);
    border-radius: 0;
    background: #ffffff;
    color: var(--color-foreground);
  }
  .s6trade-pg-modal__field input:focus {
    outline: 2px solid var(--color-foreground);
    outline-offset: -2px;
  }
  .s6trade-pg-modal__submit {
    align-self: flex-start;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--color-primary-button-background);
    color: var(--color-primary-button-text);
    border: 1.5px solid var(--color-primary-button-border);
    border-radius: 0;
    padding: 14px 28px;
    font-family: var(--font-body--family);
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    cursor: pointer;
    margin-block-start: 4px;
  }
  .s6trade-pg-modal__submit:hover {
    background: var(--color-primary-button-hover-background);
    color: var(--color-primary-button-hover-text);
    border-color: var(--color-primary-button-hover-border);
  }
  .s6trade-pg-modal__fineprint {
    font-family: var(--font-body--family);
    font-size: 12px;
    line-height: 1.5;
    color: var(--color-foreground-muted);
    margin: 4px 0 0;
  }
  .s6trade-pg-modal__error {
    font-family: var(--font-body--family);
    font-size: 14px;
    color: #a33;
    line-height: 1.4;
  }
  .s6trade-pg-modal__success {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  .s6trade-pg-modal__download {
    align-self: flex-start;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--color-primary-button-background);
    color: var(--color-primary-button-text);
    border: 1.5px solid var(--color-primary-button-border);
    border-radius: 0;
    padding: 14px 28px;
    font-family: var(--font-body--family);
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    text-decoration: none;
    margin-block-start: 4px;
  }
  .s6trade-pg-modal__download:hover {
    background: var(--color-primary-button-hover-background);
    color: var(--color-primary-button-hover-text);
    border-color: var(--color-primary-button-hover-border);
  }
  @media (max-width: 480px) {
    .s6trade-pg-modal__panel {
      padding: 24px 20px;
    }
  }
{% endstylesheet %}

<script>
  (function () {
    var modal = document.getElementById('trade-project-guide');
    if (!modal) return;

    function openModal() {
      if (typeof modal.showModal === 'function') {
        if (!modal.open) modal.showModal();
      } else {
        modal.setAttribute('open', '');
      }
    }

    function closeModal() {
      if (typeof modal.close === 'function') {
        modal.close();
      } else {
        modal.removeAttribute('open');
      }
    }

    document.addEventListener('click', function (event) {
      var trigger = event.target.closest('a[href="#trade-project-guide"]');
      if (trigger) {
        event.preventDefault();
        openModal();
        return;
      }
      if (event.target.closest('[data-trade-pg-close]')) {
        event.preventDefault();
        closeModal();
        return;
      }
      if (event.target === modal) {
        closeModal();
      }
    });

    if (modal.querySelector('[data-trade-pg-success]')) {
      openModal();
    }
  })();
</script>

{% schema %}
{
  "name": "Trade Project Guide Modal",
  "tag": "section",
  "class": "s6trade-pg-modal-wrapper",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    { "type": "header", "content": "Form copy" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Free Download" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Download Our Project Guide" },
    { "type": "richtext", "id": "description", "label": "Description", "default": "<p>Get curated art sourcing guidance for your project type.</p>" },
    { "type": "textarea", "id": "microcopy", "label": "Microcopy above fields", "default": "Tell us where to reach you and it's yours. We'll only follow up with things worth your time." },
    { "type": "text", "id": "submit_label", "label": "Submit button", "default": "DOWNLOAD GUIDE" },
    { "type": "text", "id": "fineprint", "label": "Fineprint", "default": "By submitting, you agree to receive trade emails from Society6. Unsubscribe anytime." },
    { "type": "header", "content": "Success state" },
    { "type": "text", "id": "success_heading", "label": "Success heading", "default": "Your project guide is ready" },
    { "type": "richtext", "id": "success_body", "label": "Success body", "default": "<p>Thanks — download your copy below. We'll be in touch with new collections and trade resources we think you'll want to see.</p>" },
    { "type": "text", "id": "success_link_label", "label": "Download link label", "default": "Download the Project Guide (PDF)" },
    { "type": "text", "id": "pdf_url", "label": "Project Guide PDF URL", "default": "https://lfgr-marketplaces-email-images.s3.us-east-1.amazonaws.com/society6/2026/trade-assets/Trade+Project+Guide_Brought+to+Life_Installed+Art_Spreads.pdf", "info": "Use type: text, not type: url — Shopify rejects default values on url-type settings." }
  ]
}
{% endschema %}
